import './site-source.scss';

const SiteSource = () => {
    return (
        <div className='site-source-container'>
            <button>Visit Site</button>
        </div>
    );
};

export default SiteSource;